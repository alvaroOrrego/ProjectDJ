let tl = gsap.timeline({ repeat: 0, yoyo: false });
tl.from('#logo', {
    duration: 0.5,
    rotate: 360
});

/* tl.from('.nav-link', {
    duration: 0.5,
    scale: 0,
    stagger: 0
});

tl.from('.btn-light', {
    duration: 0.5,
    scale: 0,
    stagger: 0,
    delay: 0.1
});

tl.from('#input-search', {
    duration: 0.5,
    scale: 0,
    stagger: 0,
}); */

tl.from('.text-left', {
    duration: 0.5,
    scale: 0,
    stagger: 0,
});

tl.from('#emailHelp', {
    duration: 0.5,
    scale: 0,
    stagger: 0,
});